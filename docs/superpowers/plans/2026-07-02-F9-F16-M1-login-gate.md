# F9+F16 M1（登录闸门）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让自托管 Studio 以 `IS_PLATFORM=true` 运行并获得真实登录 + API 层鉴权（仍是单项目 default、登录用户全权限）。

**Architecture:** 新增专用 platform 小栈（postgres:17 + supabase/gotrue）承载 dashboard 用户池与 `platform` 元数据 schema；Studio 前端零改动（`IS_PLATFORM` 总闸自动反转认证旁路）；`pages/api/platform/**` stub 替换为读元数据库的真实现，元数据 SQL 经现有 pg-meta 服务执行（`x-connection-encrypted` 模式，零新依赖）；`NEXT_PUBLIC_SELF_PLATFORM` 新旗标在 API 白名单闸门与 `assertSelfHosted` 上开口。

**Tech Stack:** Next.js pages router API routes、supabase/gotrue v2.189.0、pg-meta `/query`（连接串加密头）、vitest + node-mocks-http、docker compose override。

**Spec:** `docs/self-hosted-parity/2026-07-02-F9-F16-multiuser-multiproject-design.md`（§4 为 M1 详细设计）。研究依据：`docs/self-hosted-parity/2026-07-02-F9-F16-cloud-multiproject-multiuser-research.md`。

## Global Constraints

- 分支 `custom/main`；Node >= 22，pnpm 10；提交信息风格 `feat(studio): ...` / `feat(docker): ...` / `docs: ...`。
- **不得破坏纯自托管模式**：`IS_PLATFORM=false` 时所有既有行为（johndoe stub、伪登录、V1 数组形状）必须原样保留——所有新行为都以 `IS_SELF_PLATFORM`（新旗标）或 `Version: 2` 请求头分支。
- **零新增 npm 依赖**（`node-mocks-http@^1.17.2`、`crypto-js`、vitest 均已存在）。
- 上游文件改动最小化并以 `// [self-platform]` 注释标记二开边界；新代码尽量放新文件：`apps/studio/lib/constants/self-platform.ts`、`apps/studio/lib/api/self-platform/**`。
- 契约以 `packages/api-types/types/platform.d.ts` 为唯一真相源（只读不改）；响应对象用 `paths[...]`/`components['schemas'][...]` 类型标注。
- 错误响应形状：顶层 `message` 字段（前端 `handleError` 读 `error.msg ?? error.message`，`apps/studio/data/fetchers.ts:180`）。
- 首登建 profile 的触发串必须精确为 `User's profile not found`（`apps/studio/lib/profile.tsx:85` 严格相等比较）。
- 单测命令：在 `apps/studio/` 下 `pnpm exec vitest run <file>`（不要跑全量 coverage）。
- dev 环境事实（用户机器）：Studio 源码 dev server = `http://localhost:8082`（`pnpm dev:studio`）；主 docker 栈入口 = `http://localhost:8100`；docker 命令在 `docker/` 目录执行。
- 端口分配：platform-db 宿主 `5433`、platform-auth 宿主 `8110`。
- 组织 seed：slug=`default`、name=`Default Organization`、id=1；所有端点/权限响应中的 `organization_slug` 必须一致为 `default`。

## File Structure

| 文件                                                                    | 职责                                                      |
| ----------------------------------------------------------------------- | --------------------------------------------------------- |
| `apps/studio/lib/constants/self-platform.ts`（新）                      | `IS_SELF_PLATFORM` 旗标（唯一定义点，client/server 通用） |
| `apps/studio/lib/hosted-api-allowlist.ts`（改）                         | 平台模式 404 闸门对 self-platform 开口                    |
| `apps/studio/lib/api/self-hosted/util.ts`（改）                         | `assertSelfHosted` 放行 self-platform                     |
| `docker/docker-compose.platform.yml`（新）                              | platform-db + platform-auth 小栈                          |
| `docker/volumes/platform/migrations/01-schema.sql`（新）                | `platform` schema v1 + seed                               |
| `apps/studio/lib/api/self-platform/constants.ts`（新）                  | `PLATFORM_*` 服务端 env                                   |
| `apps/studio/lib/api/self-platform/db.ts`（新）                         | `executePlatformQuery`（经 pg-meta）                      |
| `apps/studio/lib/api/self-platform/profiles.ts`（新）                   | profile 行↔契约映射 + SQL                                 |
| `apps/studio/pages/api/platform/profile/index.ts`（改）                 | GET/POST profile 真实现                                   |
| `apps/studio/pages/api/platform/profile/permissions.ts`（新）           | M1 通配权限                                               |
| `apps/studio/pages/api/platform/signup.ts`（新）                        | 注册代理到 platform-auth                                  |
| `apps/studio/pages/api/platform/organizations/index.ts`（改）           | 组织列表读元数据库                                        |
| `apps/studio/pages/api/platform/organizations/[slug]/index.ts`（新）    | 组织详情                                                  |
| `apps/studio/pages/api/platform/organizations/[slug]/projects.ts`（新） | 组织项目列表（V2 分页）                                   |
| `apps/studio/pages/api/platform/projects/index.ts`（改）                | 全局项目列表 V2 形状                                      |
| `docs/self-hosted-parity/2026-07-02-M1-spike-findings.md`（新）         | Task 1 spike 产出                                         |

---

### Task 1: Spike — 平台模式失败清单（不写产品代码）

**Files:**

- Create: `docs/self-hosted-parity/2026-07-02-M1-spike-findings.md`
- Modify: `apps/studio/.env.local`（临时，spike 结束恢复）

**Interfaces:**

- Produces: 失败清单文档，含三节：`## 中间件 404`（allowlist 拦截的路径）、`## 服务端异常`（assertSelfHosted 等 throw）、`## 前端 boot 请求缺口`（网络面板里非 2xx 的 `/api/platform/*` 完整路径列表 + 调用来源 hook）。后续 Task 10/11 以此为输入修订端点清单。

- [ ] **Step 1: 备份现有 .env.local 并写入平台模式档案**

```bash
cd /Volumes/data/projects/supabase/apps/studio
cp .env.local .env.local.selfhosted.bak
```

在 `.env.local` 追加/覆盖（保留现有 `PG_META_URL`、`PG_META_CRYPTO_KEY` 等既有值）：

```bash
NEXT_PUBLIC_IS_PLATFORM=true
NEXT_PUBLIC_API_URL=http://localhost:8082/api
# spike 阶段临时借用主栈项目 GoTrue 登录（platform-auth 尚未创建）
NEXT_PUBLIC_GOTRUE_URL=http://localhost:8100/auth/v1
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=10000000-ffff-ffff-ffff-000000000001
```

- [ ] **Step 2: 在主栈 GoTrue 里创建一个测试用户（spike 专用）**

```bash
cd /Volumes/data/projects/supabase/docker
source .env
curl -s -X POST "http://localhost:8100/auth/v1/signup" \
  -H "apikey: ${ANON_KEY}" -H "Content-Type: application/json" \
  -d '{"email":"spike@internal.test","password":"spike-password-123"}'
```

Expected: 返回含 `access_token` 或 user 对象的 JSON（若 `DISABLE_SIGNUP=true` 则改用 service key 走 `/auth/v1/admin/users` 创建并确认）。

- [ ] **Step 3: 启动 dev server 并走完观察流程**

```bash
cd /Volumes/data/projects/supabase && pnpm dev:studio
```

浏览器开发者工具（Network + Console）+ dev server 终端日志，依次记录：

1. 访问 `http://localhost:8082` → 应重定向到 `/sign-in`（认证旁路已反转的证据）；
2. hCaptcha 测试 sitekey 是否正常出 token（登录按钮可提交）；
3. 用 spike 用户登录 → 记录登录后**每一个**非 2xx 的 `/api/platform/*` 请求（路径 + 状态码 + 发起的 query hook）；
4. 强行访问 `http://localhost:8082/project/default` → 记录数据页（Table Editor / SQL Editor）触发的失败请求与服务端 throw（预期见到 allowlist 404 与 `assertSelfHosted` 报错——这正是 Task 2/3 要修的）；
5. Console 里 ConfigCat / Sentry / PostHog 缺凭据时的报错级别（是否阻断渲染）。

- [ ] **Step 4: 写 spike findings 文档**

`docs/self-hosted-parity/2026-07-02-M1-spike-findings.md`，按 Interfaces 约定的三节组织；每个 404 路径标注对应的前端 hook 文件（用 `rg <path> apps/studio/data` 反查）。结论节回答：a) 数据页经 pg-meta 路由是否仅因 allowlist/assertSelfHosted 失败（Task 2/3 可解）还是另有平台形状分叉；b) hCaptcha 测试 key 是否足够（若不够，记录需要的降级点）；c) 外围 SDK 是否需要额外 env 兜底。

- [ ] **Step 5: 恢复 .env.local 并提交 findings**

```bash
cd /Volumes/data/projects/supabase/apps/studio
mv .env.local.selfhosted.bak .env.local
cd /Volumes/data/projects/supabase
git add docs/self-hosted-parity/2026-07-02-M1-spike-findings.md
git commit -m "docs: add M1 platform-mode spike findings (404 inventory)"
```

---

### Task 2: `IS_SELF_PLATFORM` 旗标 + API 白名单闸门开口

**Files:**

- Create: `apps/studio/lib/constants/self-platform.ts`
- Modify: `apps/studio/lib/hosted-api-allowlist.ts`
- Test: `apps/studio/lib/hosted-api-allowlist.test.ts`（新）

**Interfaces:**

- Produces: `IS_SELF_PLATFORM: boolean`（`@/lib/constants/self-platform`）——后续所有任务的分支旗标；`isHostedSupportedApiPath(pathname: string): boolean` 在 self-platform 下对 `/api/platform/**`、`/api/v1/**` 返回 true。

- [ ] **Step 1: 写失败测试**

`apps/studio/lib/hosted-api-allowlist.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'

async function loadAllowlist(selfPlatform: string | undefined) {
  vi.resetModules()
  if (selfPlatform === undefined) {
    vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', '')
  } else {
    vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', selfPlatform)
  }
  return await import('./hosted-api-allowlist')
}

afterEach(() => {
  vi.unstubAllEnvs()
})

describe('isHostedSupportedApiPath', () => {
  it('keeps rejecting non-allowlisted paths when self-platform is off', async () => {
    const { isHostedSupportedApiPath } = await loadAllowlist(undefined)
    expect(isHostedSupportedApiPath('/api/platform/profile')).toBe(false)
    expect(isHostedSupportedApiPath('/api/ai/docs')).toBe(true)
  })

  it('allows /api/platform/** and /api/v1/** when self-platform is on', async () => {
    const { isHostedSupportedApiPath } = await loadAllowlist('true')
    expect(isHostedSupportedApiPath('/api/platform/profile')).toBe(true)
    expect(isHostedSupportedApiPath('/api/platform/organizations/default/projects')).toBe(true)
    expect(isHostedSupportedApiPath('/api/v1/projects/default/api-keys')).toBe(true)
    expect(isHostedSupportedApiPath('/api/ai/docs')).toBe(true)
  })

  it('still rejects unrelated api paths when self-platform is on', async () => {
    const { isHostedSupportedApiPath } = await loadAllowlist('true')
    expect(isHostedSupportedApiPath('/api/totally-unknown')).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/hosted-api-allowlist.test.ts`
Expected: FAIL（self-platform on 的两条断言不成立）

- [ ] **Step 3: 实现**

新建 `apps/studio/lib/constants/self-platform.ts`：

```ts
// [self-platform] Single definition of the self-hosted-platform-mode flag.
// True only when this Studio build runs its own in-repo platform API
// (IS_PLATFORM=true against pages/api/platform/**). Build-time inlined.
export const IS_SELF_PLATFORM = process.env.NEXT_PUBLIC_SELF_PLATFORM === 'true'
```

`apps/studio/lib/hosted-api-allowlist.ts` 的 `isHostedSupportedApiPath` 改为：

```ts
// [self-platform] Local platform API needs its own /api/platform + /api/v1
// routes reachable in platform mode; everything else keeps 404ing.
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export function isHostedSupportedApiPath(pathname: string): boolean {
  if (IS_SELF_PLATFORM && (pathname.includes('/api/platform/') || pathname.includes('/api/v1/'))) {
    return true
  }
  return HOSTED_SUPPORTED_API_URLS.some((url) => pathname.endsWith(url))
}
```

注意：`self-platform.ts` 读的是构建期内联 env；测试里用 `vi.resetModules()` + `stubEnv` 重载。若 `@/lib/constants/self-platform` 在 allowlist 文件里 import 造成 middleware bundle 问题（edge runtime），改为在 allowlist 文件内直接 `process.env.NEXT_PUBLIC_SELF_PLATFORM === 'true'` 并在注释里指向唯一定义点。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run lib/hosted-api-allowlist.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add apps/studio/lib/constants/self-platform.ts apps/studio/lib/hosted-api-allowlist.ts apps/studio/lib/hosted-api-allowlist.test.ts
git commit -m "feat(studio): open platform-mode api gate for self-platform builds"
```

---

### Task 3: `assertSelfHosted` 放行 self-platform

**Files:**

- Modify: `apps/studio/lib/api/self-hosted/util.ts:17-21`
- Test: `apps/studio/lib/api/self-hosted/util.test.ts`（新）

**Interfaces:**

- Consumes: `IS_SELF_PLATFORM`（Task 2）
- Produces: `assertSelfHosted()` 在 `IS_PLATFORM && IS_SELF_PLATFORM` 时不再 throw——解锁全部 pg-meta/self-hosted 数据路由。

- [ ] **Step 1: 写失败测试**

`apps/studio/lib/api/self-hosted/util.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'

async function loadUtil(env: { isPlatform: string; selfPlatform: string }) {
  vi.resetModules()
  vi.stubEnv('NEXT_PUBLIC_IS_PLATFORM', env.isPlatform)
  vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', env.selfPlatform)
  return await import('./util')
}

afterEach(() => {
  vi.unstubAllEnvs()
})

describe('assertSelfHosted', () => {
  it('passes in plain self-hosted mode', async () => {
    const { assertSelfHosted } = await loadUtil({ isPlatform: 'false', selfPlatform: '' })
    expect(() => assertSelfHosted()).not.toThrow()
  })

  it('throws in platform mode without self-platform', async () => {
    const { assertSelfHosted } = await loadUtil({ isPlatform: 'true', selfPlatform: '' })
    expect(() => assertSelfHosted()).toThrow()
  })

  it('passes in self-platform mode', async () => {
    const { assertSelfHosted } = await loadUtil({ isPlatform: 'true', selfPlatform: 'true' })
    expect(() => assertSelfHosted()).not.toThrow()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-hosted/util.test.ts`
Expected: FAIL（第 3 条：self-platform 下仍 throw）

- [ ] **Step 3: 实现**

`apps/studio/lib/api/self-hosted/util.ts`：

```ts
import { IS_PLATFORM } from '@/lib/constants'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

/**
 * Asserts that the current environment is self-hosted.
 * [self-platform] Self-platform builds run IS_PLATFORM=true against the
 * local single-project stack, so they count as self-hosted here.
 */
export function assertSelfHosted() {
  if (IS_PLATFORM && !IS_SELF_PLATFORM) {
    throw new Error('This function can only be called in self-hosted environments')
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-hosted/util.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add apps/studio/lib/api/self-hosted/util.ts apps/studio/lib/api/self-hosted/util.test.ts
git commit -m "feat(studio): allow self-hosted db helpers in self-platform mode"
```

---

### Task 4: platform 小栈（compose + 元数据迁移 v1）

**Files:**

- Create: `docker/docker-compose.platform.yml`
- Create: `docker/volumes/platform/migrations/01-schema.sql`
- Modify: `docker/.gitignore`（追加 data 目录）、`docker/.env`（本机追加两个变量，不入库）

**Interfaces:**

- Produces: 运行中的 `platform-db`（宿主 5433，库名 `platform`，schema `platform` + seed default org）与 `platform-auth`（宿主 8110，GoTrue，自动迁移 `auth` schema）。env 约定：`PLATFORM_POSTGRES_PASSWORD`、`PLATFORM_JWT_SECRET`（≥32 字符）。

- [ ] **Step 1: 写迁移 SQL**

`docker/volumes/platform/migrations/01-schema.sql`：

```sql
-- Platform control-plane metadata, v1 (F9+F16 M1).
-- GoTrue owns the `auth` schema (auto-migrates on start); pre-create it so
-- the migration user does not need CREATE on the database.
create schema if not exists auth;

create schema if not exists platform;

create table platform.organizations (
  id bigint generated always as identity primary key,
  slug text not null unique,
  name text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table platform.profiles (
  id bigint generated always as identity primary key,
  gotrue_id uuid not null unique,
  username text not null,
  primary_email text not null,
  first_name text,
  last_name text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table platform.organization_members (
  organization_id bigint not null references platform.organizations (id) on delete cascade,
  profile_id bigint not null references platform.profiles (id) on delete cascade,
  created_at timestamptz not null default now(),
  primary key (organization_id, profile_id)
);

insert into platform.organizations (slug, name)
values ('default', 'Default Organization');
```

- [ ] **Step 2: 写 compose override**

`docker/docker-compose.platform.yml`：

```yaml
# Platform control-plane mini-stack (F9+F16 M1): metadata db + dashboard auth.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.platform.yml up -d platform-db platform-auth
# Requires in .env: PLATFORM_POSTGRES_PASSWORD, PLATFORM_JWT_SECRET (>= 32 chars)

services:
  platform-db:
    container_name: supabase-platform-db
    image: postgres:17-alpine
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'pg_isready', '-U', 'postgres', '-d', 'platform']
      interval: 5s
      timeout: 5s
      retries: 10
    environment:
      POSTGRES_DB: platform
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${PLATFORM_POSTGRES_PASSWORD}
    ports:
      - '${PLATFORM_POSTGRES_HOST_PORT:-5433}:5432'
    volumes:
      - ./volumes/platform/db/data:/var/lib/postgresql/data:Z
      - ./volumes/platform/migrations:/docker-entrypoint-initdb.d:Z

  platform-auth:
    container_name: supabase-platform-auth
    image: supabase/gotrue:v2.189.0
    restart: unless-stopped
    depends_on:
      platform-db:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:9999/health']
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - '${PLATFORM_GOTRUE_HOST_PORT:-8110}:9999'
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      API_EXTERNAL_URL: http://localhost:${PLATFORM_GOTRUE_HOST_PORT:-8110}

      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://postgres:${PLATFORM_POSTGRES_PASSWORD}@platform-db:5432/platform

      GOTRUE_SITE_URL: ${PLATFORM_SITE_URL:-http://localhost:8082}
      GOTRUE_URI_ALLOW_LIST: ${PLATFORM_SITE_URL:-http://localhost:8082}/**
      GOTRUE_DISABLE_SIGNUP: 'false'

      GOTRUE_JWT_ADMIN_ROLES: service_role
      GOTRUE_JWT_AUD: authenticated
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated
      GOTRUE_JWT_EXP: ${PLATFORM_JWT_EXPIRY:-3600}
      GOTRUE_JWT_SECRET: ${PLATFORM_JWT_SECRET}

      # M1: no SMTP — email signup with autoconfirm (spec §4.1; tighten in M3)
      GOTRUE_EXTERNAL_EMAIL_ENABLED: 'true'
      GOTRUE_MAILER_AUTOCONFIRM: 'true'
```

镜像版本对齐主 compose 的 `supabase/gotrue`（`docker-compose.yml:116`，当前 v2.189.0；若已升级则取主文件同值）。

- [ ] **Step 3: gitignore data 目录 + 本机 .env 追加**

`docker/.gitignore` 追加一行（若该文件不存在则看仓库根 `.gitignore` 中 `volumes/db/data` 的既有写法并跟随）：

```
volumes/platform/db/data
```

本机 `docker/.env` 追加（不入库）：

```bash
PLATFORM_POSTGRES_PASSWORD=$(openssl rand -hex 16)
PLATFORM_JWT_SECRET=$(openssl rand -hex 32)
```

- [ ] **Step 4: 启动并验证**

```bash
cd /Volumes/data/projects/supabase/docker
docker compose -f docker-compose.yml -f docker-compose.platform.yml up -d platform-db platform-auth
docker compose -f docker-compose.yml -f docker-compose.platform.yml ps platform-db platform-auth
```

Expected: 两服务 `healthy`。继续验证：

```bash
# GoTrue 健康 + 注册一个用户
curl -s http://localhost:8110/health
curl -s -X POST http://localhost:8110/signup -H 'Content-Type: application/json' \
  -d '{"email":"admin@internal.test","password":"admin-password-123"}'
# 元数据 schema 与 seed
docker exec supabase-platform-db psql -U postgres -d platform \
  -c "select slug, name from platform.organizations;" \
  -c "select count(*) from auth.users;"
```

Expected: `/health` 返回 JSON；signup 返回含 `access_token` 的 session（autoconfirm）；organizations 有 `default | Default Organization`；`auth.users` count = 1。

- [ ] **Step 5: Commit**

```bash
git add docker/docker-compose.platform.yml docker/volumes/platform/migrations/01-schema.sql docker/.gitignore
git commit -m "feat(docker): add platform control-plane mini-stack (postgres + gotrue)"
```

---

### Task 5: 平台元数据查询助手 `executePlatformQuery`

**Files:**

- Create: `apps/studio/lib/api/self-platform/constants.ts`
- Create: `apps/studio/lib/api/self-platform/db.ts`
- Test: `apps/studio/lib/api/self-platform/db.test.ts`

**Interfaces:**

- Consumes: `encryptString`（`@/lib/api/self-hosted/util`）、`PG_META_URL`（`@/lib/constants`）、`constructHeaders`（`@/lib/api/apiHelpers`）
- Produces:
  - `executePlatformQuery<T>(opts: { query: string; parameters?: unknown[] }): Promise<{ data: T[] | undefined; error: Error | undefined }>`
  - `getPlatformConnectionString(): string`
  - `PLATFORM_GOTRUE_URL: string`（Task 7 signup 用）

- [ ] **Step 1: 写失败测试**

`apps/studio/lib/api/self-platform/db.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'

async function loadDb() {
  vi.resetModules()
  vi.stubEnv('PLATFORM_POSTGRES_HOST', 'platform-db')
  vi.stubEnv('PLATFORM_POSTGRES_PORT', '5432')
  vi.stubEnv('PLATFORM_POSTGRES_DB', 'platform')
  vi.stubEnv('PLATFORM_POSTGRES_USER', 'postgres')
  vi.stubEnv('PLATFORM_POSTGRES_PASSWORD', 'pw123')
  return await import('./db')
}

afterEach(() => {
  vi.unstubAllEnvs()
  vi.unstubAllGlobals()
})

describe('getPlatformConnectionString', () => {
  it('builds the connection string from PLATFORM_* env', async () => {
    const { getPlatformConnectionString } = await loadDb()
    expect(getPlatformConnectionString()).toBe(
      'postgresql://postgres:pw123@platform-db:5432/platform'
    )
  })
})

describe('executePlatformQuery', () => {
  it('POSTs to pg-meta /query with encrypted connection header and parameters', async () => {
    const fetchMock = vi
      .fn()
      .mockResolvedValue(new Response(JSON.stringify([{ ok: 1 }]), { status: 200 }))
    vi.stubGlobal('fetch', fetchMock)
    const { executePlatformQuery } = await loadDb()

    const { data, error } = await executePlatformQuery<{ ok: number }>({
      query: 'select 1 as ok where $1 = $1',
      parameters: ['x'],
    })

    expect(error).toBeUndefined()
    expect(data).toEqual([{ ok: 1 }])
    const [url, init] = fetchMock.mock.calls[0]
    expect(String(url)).toMatch(/\/query$/)
    const headers = new Headers(init.headers)
    expect(headers.get('x-connection-encrypted')).toBeTruthy()
    expect(JSON.parse(init.body)).toEqual({
      query: 'select 1 as ok where $1 = $1',
      parameters: ['x'],
    })
  })

  it('returns error on non-ok response', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue(new Response(JSON.stringify({ message: 'boom' }), { status: 500 }))
    )
    const { executePlatformQuery } = await loadDb()
    const { data, error } = await executePlatformQuery({ query: 'select 1' })
    expect(data).toBeUndefined()
    expect(error?.message).toContain('boom')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/db.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`apps/studio/lib/api/self-platform/constants.ts`：

```ts
// [self-platform] Server-side env for the platform control-plane stack.
// Hosts are as seen FROM the pg-meta container (docker network DNS).
export const PLATFORM_POSTGRES_HOST = process.env.PLATFORM_POSTGRES_HOST || 'platform-db'
export const PLATFORM_POSTGRES_PORT = parseInt(process.env.PLATFORM_POSTGRES_PORT || '5432', 10)
export const PLATFORM_POSTGRES_DB = process.env.PLATFORM_POSTGRES_DB || 'platform'
export const PLATFORM_POSTGRES_USER = process.env.PLATFORM_POSTGRES_USER || 'postgres'
export const PLATFORM_POSTGRES_PASSWORD = process.env.PLATFORM_POSTGRES_PASSWORD || ''
// As seen from the Studio server process (host in dev, container in docker).
export const PLATFORM_GOTRUE_URL = process.env.PLATFORM_GOTRUE_URL || 'http://localhost:8110'
```

`apps/studio/lib/api/self-platform/db.ts`：

```ts
// [self-platform] Executes SQL against the platform metadata db via the
// existing pg-meta service (same x-connection-encrypted mechanism as
// lib/api/self-hosted/query.ts, different connection target).
import { constructHeaders } from '../apiHelpers'
import { encryptString } from '../self-hosted/util'
import {
  PLATFORM_POSTGRES_DB,
  PLATFORM_POSTGRES_HOST,
  PLATFORM_POSTGRES_PASSWORD,
  PLATFORM_POSTGRES_PORT,
  PLATFORM_POSTGRES_USER,
} from './constants'
import { PG_META_URL } from '@/lib/constants/index'

export function getPlatformConnectionString(): string {
  return `postgresql://${PLATFORM_POSTGRES_USER}:${PLATFORM_POSTGRES_PASSWORD}@${PLATFORM_POSTGRES_HOST}:${PLATFORM_POSTGRES_PORT}/${PLATFORM_POSTGRES_DB}`
}

export type PlatformQueryOptions = {
  query: string
  parameters?: unknown[]
}

export async function executePlatformQuery<T = unknown>({
  query,
  parameters,
}: PlatformQueryOptions): Promise<{ data: T[] | undefined; error: Error | undefined }> {
  const connectionStringEncrypted = encryptString(getPlatformConnectionString())

  const requestBody: { query: string; parameters?: unknown[] } = { query }
  if (parameters !== undefined) {
    requestBody.parameters = parameters
  }

  const response = await fetch(`${PG_META_URL}/query`, {
    method: 'POST',
    headers: constructHeaders({
      'Content-Type': 'application/json',
      'x-connection-encrypted': connectionStringEncrypted,
    }),
    body: JSON.stringify(requestBody),
  })

  const result = await response.json()
  if (!response.ok) {
    const message = typeof result?.message === 'string' ? result.message : JSON.stringify(result)
    return { data: undefined, error: new Error(message) }
  }
  return { data: result as T[], error: undefined }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/db.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 5: 真实栈冒烟（需 Task 4 的栈与主栈 pg-meta 在运行）**

```bash
cd /Volumes/data/projects/supabase/apps/studio
node --input-type=module -e "
process.env.NEXT_PUBLIC_IS_PLATFORM='true'; process.env.NEXT_PUBLIC_SELF_PLATFORM='true';
" 2>/dev/null || true
```

冒烟改为通过 Task 6 完成后的 API 手测覆盖（pg-meta 容器需能解析 `platform-db`：两栈共用 compose 项目网络，Task 4 已保证）。此步只需确认 pg-meta 与 platform-db 同网络：

```bash
cd /Volumes/data/projects/supabase/docker
docker exec supabase-meta getent hosts platform-db
```

Expected: 输出 platform-db 的容器 IP（容器名以 `docker compose ps` 实际名称为准）。

- [ ] **Step 6: Commit**

```bash
git add apps/studio/lib/api/self-platform/
git commit -m "feat(studio): add platform metadata query helper via pg-meta"
```

---

### Task 6: `/platform/profile` GET/POST 真实现（首登自动建档）

**Files:**

- Create: `apps/studio/lib/api/self-platform/profiles.ts`
- Modify: `apps/studio/pages/api/platform/profile/index.ts`
- Test: `apps/studio/lib/api/self-platform/profiles.test.ts`、`apps/studio/pages/api/platform/profile/index.test.ts`

**Interfaces:**

- Consumes: `executePlatformQuery`（Task 5）、`IS_SELF_PLATFORM`（Task 2）、`apiWrapper` 传入的 `claims: JwtPayload`（`claims.sub` = gotrue_id，`claims.email`）
- Produces:
  - `type PlatformProfileRow = { id: number; gotrue_id: string; username: string; primary_email: string; first_name: string | null; last_name: string | null }`
  - `toProfileResponse(row: PlatformProfileRow): components['schemas']['ProfileResponse']`
  - `getProfileByGotrueId(gotrueId: string): Promise<PlatformProfileRow | null>`
  - `createProfileWithDefaultMembership(input: { gotrueId: string; email: string }): Promise<PlatformProfileRow>`
  - HTTP 契约：GET 200 `ProfileResponse` / 404 `{ message: "User's profile not found" }`；POST 201 `ProfileResponse`（幂等：已存在返回现行）

- [ ] **Step 1: 写失败测试（lib 层）**

`apps/studio/lib/api/self-platform/profiles.test.ts`：

```ts
import { describe, expect, it, vi } from 'vitest'

import { executePlatformQuery } from './db'
import {
  createProfileWithDefaultMembership,
  getProfileByGotrueId,
  toProfileResponse,
} from './profiles'

vi.mock('./db', () => ({ executePlatformQuery: vi.fn() }))

const row = {
  id: 7,
  gotrue_id: '4c1e58f1-9d90-4f66-9b7e-000000000001',
  username: 'admin',
  primary_email: 'admin@internal.test',
  first_name: null,
  last_name: null,
}

describe('toProfileResponse', () => {
  it('maps a platform.profiles row onto the ProfileResponse contract', () => {
    const res = toProfileResponse(row)
    expect(res).toMatchObject({
      id: 7,
      gotrue_id: row.gotrue_id,
      username: 'admin',
      primary_email: 'admin@internal.test',
      first_name: null,
      last_name: null,
      mobile: null,
      is_alpha_user: false,
      is_sso_user: false,
      disabled_features: [],
    })
    expect(typeof res.auth0_id).toBe('string')
  })
})

describe('getProfileByGotrueId', () => {
  it('returns null when no row', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({ data: [], error: undefined })
    expect(await getProfileByGotrueId(row.gotrue_id)).toBeNull()
  })

  it('passes gotrue_id as a bind parameter', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({ data: [row], error: undefined })
    await getProfileByGotrueId(row.gotrue_id)
    const call = vi.mocked(executePlatformQuery).mock.calls.at(-1)![0]
    expect(call.parameters).toEqual([row.gotrue_id])
    expect(call.query).not.toContain(row.gotrue_id)
  })
})

describe('createProfileWithDefaultMembership', () => {
  it('derives username from the email local part', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({ data: [row], error: undefined })
    await createProfileWithDefaultMembership({
      gotrueId: row.gotrue_id,
      email: 'admin@internal.test',
    })
    const call = vi.mocked(executePlatformQuery).mock.calls.at(-1)![0]
    expect(call.parameters).toEqual([row.gotrue_id, 'admin', 'admin@internal.test'])
  })

  it('throws when the query errors', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({
      data: undefined,
      error: new Error('db down'),
    })
    await expect(
      createProfileWithDefaultMembership({ gotrueId: row.gotrue_id, email: 'a@b.c' })
    ).rejects.toThrow('db down')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/profiles.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 lib 层**

`apps/studio/lib/api/self-platform/profiles.ts`：

```ts
// [self-platform] platform.profiles data access + ProfileResponse mapping.
import type { components } from 'api-types'

import { executePlatformQuery } from './db'

export type PlatformProfileRow = {
  id: number
  gotrue_id: string
  username: string
  primary_email: string
  first_name: string | null
  last_name: string | null
}

type ProfileResponse = components['schemas']['ProfileResponse']

export function toProfileResponse(row: PlatformProfileRow): ProfileResponse {
  return {
    id: row.id,
    gotrue_id: row.gotrue_id,
    // No auth0 legacy ids on self-platform; prefix keeps GitHub-avatar
    // detection (auth0_id.startsWith('github')) inert.
    auth0_id: `email|${row.gotrue_id}`,
    username: row.username,
    primary_email: row.primary_email,
    first_name: row.first_name,
    last_name: row.last_name,
    mobile: null,
    is_alpha_user: false,
    is_sso_user: false,
    free_project_limit: 10,
    disabled_features: [],
  }
}

export async function getProfileByGotrueId(gotrueId: string): Promise<PlatformProfileRow | null> {
  const { data, error } = await executePlatformQuery<PlatformProfileRow>({
    query: `
      select id, gotrue_id, username, primary_email, first_name, last_name
      from platform.profiles
      where gotrue_id = $1
    `,
    parameters: [gotrueId],
  })
  if (error) throw error
  return data?.[0] ?? null
}

export async function createProfileWithDefaultMembership(input: {
  gotrueId: string
  email: string
}): Promise<PlatformProfileRow> {
  const username = input.email.split('@')[0] || input.email
  const { data, error } = await executePlatformQuery<PlatformProfileRow>({
    query: `
      with new_profile as (
        insert into platform.profiles (gotrue_id, username, primary_email)
        values ($1, $2, $3)
        on conflict (gotrue_id) do update set updated_at = now()
        returning id, gotrue_id, username, primary_email, first_name, last_name
      ), membership as (
        insert into platform.organization_members (organization_id, profile_id)
        select o.id, p.id
        from platform.organizations o
        cross join new_profile p
        where o.slug = 'default'
        on conflict do nothing
      )
      select * from new_profile
    `,
    parameters: [input.gotrueId, username, input.email],
  })
  if (error) throw error
  if (!data?.[0]) throw new Error('profile creation returned no row')
  return data[0]
}
```

- [ ] **Step 4: 跑 lib 测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/profiles.test.ts`
Expected: PASS（6 tests）

- [ ] **Step 5: 写失败测试（handler 层，node-mocks-http）**

`apps/studio/pages/api/platform/profile/index.test.ts`：

```ts
import { createMocks } from 'node-mocks-http'
import { beforeEach, describe, expect, it, vi } from 'vitest'

import { handler } from './index'
import {
  createProfileWithDefaultMembership,
  getProfileByGotrueId,
} from '@/lib/api/self-platform/profiles'

vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', 'true')
vi.mock('@/lib/api/self-platform/profiles', () => ({
  getProfileByGotrueId: vi.fn(),
  createProfileWithDefaultMembership: vi.fn(),
  toProfileResponse: (row: any) => ({ ...row, mapped: true }),
}))

const claims = { sub: '4c1e58f1-9d90-4f66-9b7e-000000000001', email: 'admin@internal.test' }
const row = { id: 7, gotrue_id: claims.sub, username: 'admin', primary_email: claims.email }

beforeEach(() => vi.clearAllMocks())

describe('GET /platform/profile (self-platform)', () => {
  it('returns 404 with the exact frontend trigger message when profile missing', async () => {
    vi.mocked(getProfileByGotrueId).mockResolvedValue(null)
    const { req, res } = createMocks({ method: 'GET' })
    await handler(req as any, res as any, claims as any)
    expect(res._getStatusCode()).toBe(404)
    expect(res._getJSONData()).toEqual({ message: "User's profile not found" })
  })

  it('returns the mapped profile when found', async () => {
    vi.mocked(getProfileByGotrueId).mockResolvedValue(row as any)
    const { req, res } = createMocks({ method: 'GET' })
    await handler(req as any, res as any, claims as any)
    expect(res._getStatusCode()).toBe(200)
    expect(res._getJSONData()).toMatchObject({ id: 7, mapped: true })
  })
})

describe('POST /platform/profile (self-platform)', () => {
  it('creates profile + default membership from claims', async () => {
    vi.mocked(createProfileWithDefaultMembership).mockResolvedValue(row as any)
    const { req, res } = createMocks({ method: 'POST' })
    await handler(req as any, res as any, claims as any)
    expect(createProfileWithDefaultMembership).toHaveBeenCalledWith({
      gotrueId: claims.sub,
      email: claims.email,
    })
    expect(res._getStatusCode()).toBe(201)
  })
})
```

- [ ] **Step 6: 实现 handler**

重写 `apps/studio/pages/api/platform/profile/index.ts`：

```ts
import type { JwtPayload } from '@supabase/supabase-js'
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import {
  createProfileWithDefaultMembership,
  getProfileByGotrueId,
  toProfileResponse,
} from '@/lib/api/self-platform/profiles'
import { DEFAULT_PROJECT } from '@/lib/constants/api'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

// [self-platform] exported for handler-level tests
export async function handler(req: NextApiRequest, res: NextApiResponse, claims?: JwtPayload) {
  if (!IS_SELF_PLATFORM) return legacyHandler(req, res)

  const gotrueId = claims?.sub
  const email = (claims as { email?: string } | undefined)?.email
  if (!gotrueId || !email) {
    return res.status(401).json({ message: 'Unauthorized: missing token claims' })
  }

  switch (req.method) {
    case 'GET': {
      const row = await getProfileByGotrueId(gotrueId)
      if (!row) return res.status(404).json({ message: "User's profile not found" })
      return res.status(200).json(toProfileResponse(row))
    }
    case 'POST': {
      const row = await createProfileWithDefaultMembership({ gotrueId, email })
      return res.status(201).json(toProfileResponse(row))
    }
    default:
      res.setHeader('Allow', ['GET', 'POST'])
      return res
        .status(405)
        .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
}

// Plain self-hosted keeps the historical stub payload untouched.
async function legacyHandler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
  const response = {
    id: 1,
    primary_email: 'johndoe@supabase.io',
    username: 'johndoe',
    first_name: 'John',
    last_name: 'Doe',
    organizations: [
      {
        id: 1,
        name: process.env.DEFAULT_ORGANIZATION_NAME || 'Default Organization',
        slug: 'default-org-slug',
        billing_email: 'billing@supabase.co',
        projects: [{ ...DEFAULT_PROJECT, connectionString: '' }],
      },
    ],
  }
  return res.status(200).json(response)
}
```

- [ ] **Step 7: 跑 handler 测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/profile/index.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 8: Commit**

```bash
git add apps/studio/lib/api/self-platform/profiles.ts apps/studio/lib/api/self-platform/profiles.test.ts "apps/studio/pages/api/platform/profile/index.ts" "apps/studio/pages/api/platform/profile/index.test.ts"
git commit -m "feat(studio): real /platform/profile backed by platform metadata db"
```

---

### Task 7: `POST /platform/signup`

**Files:**

- Create: `apps/studio/pages/api/platform/signup.ts`
- Test: `apps/studio/pages/api/platform/signup.test.ts`

**Interfaces:**

- Consumes: `PLATFORM_GOTRUE_URL`（Task 5 constants）；请求体 `{ email, password, hcaptchaToken, redirectTo }`（`data/misc/signup-mutation.ts:15-23` 已固定）
- Produces: 200 透传 GoTrue session/user JSON；GoTrue 错误按其状态码 + `{ message }` 透传。profile 行不在此创建（首登 GET 404 → 前端自动 POST /platform/profile，Task 6 已覆盖）。

- [ ] **Step 1: 写失败测试**

`apps/studio/pages/api/platform/signup.test.ts`：

```ts
import { createMocks } from 'node-mocks-http'
import { afterEach, describe, expect, it, vi } from 'vitest'

import { handler } from './signup'

vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', 'true')

afterEach(() => vi.unstubAllGlobals())

describe('POST /platform/signup (self-platform)', () => {
  it('forwards email/password to platform gotrue /signup', async () => {
    const fetchMock = vi
      .fn()
      .mockResolvedValue(new Response(JSON.stringify({ id: 'u1' }), { status: 200 }))
    vi.stubGlobal('fetch', fetchMock)

    const { req, res } = createMocks({
      method: 'POST',
      body: { email: 'a@b.c', password: 'pw', hcaptchaToken: null, redirectTo: '/' },
    })
    await handler(req as any, res as any)

    const [url, init] = fetchMock.mock.calls[0]
    expect(String(url)).toMatch(/\/signup$/)
    expect(JSON.parse(init.body)).toEqual({ email: 'a@b.c', password: 'pw' })
    expect(res._getStatusCode()).toBe(200)
  })

  it('maps gotrue errors onto { message } with original status', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue(
        new Response(JSON.stringify({ code: 422, msg: 'User already registered' }), {
          status: 422,
        })
      )
    )
    const { req, res } = createMocks({
      method: 'POST',
      body: { email: 'a@b.c', password: 'pw', hcaptchaToken: null, redirectTo: '/' },
    })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(422)
    expect(res._getJSONData()).toEqual({ message: 'User already registered' })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/signup.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`apps/studio/pages/api/platform/signup.ts`：

```ts
// [self-platform] Dashboard signup: proxy to the platform GoTrue /signup.
// Frontend body: { email, password, hcaptchaToken, redirectTo }
// (data/misc/signup-mutation.ts). Captcha is not enforced on self-platform.
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import { PLATFORM_GOTRUE_URL } from '@/lib/api/self-platform/constants'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) => apiWrapper(req, res, handler)

// exported for handler-level tests
export async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (!IS_SELF_PLATFORM) {
    return res.status(404).json({ message: 'Signup is not available on this deployment' })
  }
  if (req.method !== 'POST') {
    res.setHeader('Allow', ['POST'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }

  const { email, password } = req.body ?? {}
  if (typeof email !== 'string' || typeof password !== 'string') {
    return res.status(400).json({ message: 'email and password are required' })
  }

  const response = await fetch(`${PLATFORM_GOTRUE_URL}/signup`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password }),
  })
  const result = await response.json()

  if (!response.ok) {
    const message =
      typeof result?.msg === 'string'
        ? result.msg
        : typeof result?.message === 'string'
          ? result.message
          : 'Signup failed'
    return res.status(response.status).json({ message })
  }
  return res.status(200).json(result)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/signup.test.ts`
Expected: PASS（2 tests）

- [ ] **Step 5: Commit**

```bash
git add apps/studio/pages/api/platform/signup.ts apps/studio/pages/api/platform/signup.test.ts
git commit -m "feat(studio): dashboard signup endpoint proxying platform gotrue"
```

---

### Task 8: `GET /platform/profile/permissions`（M1 通配权限）

**Files:**

- Create: `apps/studio/pages/api/platform/profile/permissions.ts`
- Test: `apps/studio/pages/api/platform/profile/permissions.test.ts`

**Interfaces:**

- Produces: 200 → `AccessControlPermission[]`，单条通配（M3 将换成按角色展开）。形状严格对齐 `packages/api-types/types/platform.d.ts:4887`（`AccessControlPermission`）与前端评估器（`hooks/misc/useCheckPermissions.ts`：`%` 通配、`organization_slug` 分区、空 `project_refs` = 组织级）。

- [ ] **Step 1: 写失败测试**

`apps/studio/pages/api/platform/profile/permissions.test.ts`：

```ts
import { createMocks } from 'node-mocks-http'
import { describe, expect, it, vi } from 'vitest'

import { handler } from './permissions'

vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', 'true')

describe('GET /platform/profile/permissions (self-platform, M1)', () => {
  it('returns a single org-wide wildcard grant for the default org', async () => {
    const { req, res } = createMocks({ method: 'GET' })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(200)
    expect(res._getJSONData()).toEqual([
      {
        actions: ['%'],
        condition: null,
        organization_id: 1,
        organization_slug: 'default',
        project_ids: [],
        project_refs: [],
        resources: ['%'],
        restrictive: false,
      },
    ])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/profile/permissions.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`apps/studio/pages/api/platform/profile/permissions.ts`：

```ts
// [self-platform] M1 transitional RBAC: every authenticated dashboard user
// gets an org-wide wildcard grant on the default org. Replaced in M3 by the
// role -> AccessControlPermission expansion (spec §6).
import type { components } from 'api-types'
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

type AccessControlPermission = components['schemas']['AccessControlPermission']

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

// exported for handler-level tests
export async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (!IS_SELF_PLATFORM) {
    return res.status(404).json({ message: 'Not available on this deployment' })
  }
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }

  const permissions: AccessControlPermission[] = [
    {
      actions: ['%'],
      condition: null,
      organization_id: 1,
      organization_slug: 'default',
      project_ids: [],
      project_refs: [],
      resources: ['%'],
      restrictive: false,
    },
  ]
  return res.status(200).json(permissions)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/profile/permissions.test.ts`
Expected: PASS（1 test）

- [ ] **Step 5: Commit**

```bash
git add apps/studio/pages/api/platform/profile/permissions.ts apps/studio/pages/api/platform/profile/permissions.test.ts
git commit -m "feat(studio): M1 wildcard permissions endpoint for self-platform"
```

---

### Task 9: 组织端点（列表读库 + 详情新增）

**Files:**

- Create: `apps/studio/lib/api/self-platform/organizations.ts`
- Modify: `apps/studio/pages/api/platform/organizations/index.ts`
- Create: `apps/studio/pages/api/platform/organizations/[slug]/index.ts`
- Test: `apps/studio/lib/api/self-platform/organizations.test.ts`

**Interfaces:**

- Consumes: `executePlatformQuery`（Task 5）
- Produces:
  - `type PlatformOrganizationRow = { id: number; slug: string; name: string }`
  - `listOrganizations(): Promise<PlatformOrganizationRow[]>`、`getOrganizationBySlug(slug: string): Promise<PlatformOrganizationRow | null>`
  - `toOrganizationResponse(row)` → `components['schemas']['OrganizationResponse']`（`plan: {id:'enterprise', name:'Enterprise'}`、`is_owner: true`、其余可空字段取 null/false/[]）
  - `toOrganizationSlugResponse(row)` → `components['schemas']['OrganizationSlugResponse']`（含 `has_oriole_project: false`）

- [ ] **Step 1: 写失败测试**

`apps/studio/lib/api/self-platform/organizations.test.ts`：

```ts
import { describe, expect, it, vi } from 'vitest'

import { executePlatformQuery } from './db'
import {
  getOrganizationBySlug,
  listOrganizations,
  toOrganizationResponse,
  toOrganizationSlugResponse,
} from './organizations'

vi.mock('./db', () => ({ executePlatformQuery: vi.fn() }))

const row = { id: 1, slug: 'default', name: 'Default Organization' }

describe('toOrganizationResponse', () => {
  it('produces the OrganizationResponse contract with enterprise plan', () => {
    expect(toOrganizationResponse(row)).toMatchObject({
      id: 1,
      slug: 'default',
      name: 'Default Organization',
      is_owner: true,
      plan: { id: 'enterprise', name: 'Enterprise' },
      opt_in_tags: [],
      billing_email: null,
      restriction_status: null,
    })
  })
})

describe('toOrganizationSlugResponse', () => {
  it('includes has_oriole_project and drops list-only fields', () => {
    const res = toOrganizationSlugResponse(row)
    expect(res).toMatchObject({ slug: 'default', has_oriole_project: false })
    expect('is_owner' in res).toBe(false)
  })
})

describe('queries', () => {
  it('listOrganizations selects all rows', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({ data: [row], error: undefined })
    expect(await listOrganizations()).toEqual([row])
  })

  it('getOrganizationBySlug binds the slug parameter', async () => {
    vi.mocked(executePlatformQuery).mockResolvedValue({ data: [], error: undefined })
    expect(await getOrganizationBySlug('default')).toBeNull()
    const call = vi.mocked(executePlatformQuery).mock.calls.at(-1)![0]
    expect(call.parameters).toEqual(['default'])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/organizations.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 lib 层**

`apps/studio/lib/api/self-platform/organizations.ts`：

```ts
// [self-platform] platform.organizations data access + contract mapping.
// Plan is hard-coded 'enterprise' so plan/entitlement-gated UI stays open
// (spec §4.5); billing fields are nulled — self-platform has no billing.
import type { components } from 'api-types'

import { executePlatformQuery } from './db'

export type PlatformOrganizationRow = { id: number; slug: string; name: string }

type OrganizationResponse = components['schemas']['OrganizationResponse']
type OrganizationSlugResponse = components['schemas']['OrganizationSlugResponse']

const ENTERPRISE_PLAN = { id: 'enterprise' as const, name: 'Enterprise' }

export function toOrganizationResponse(row: PlatformOrganizationRow): OrganizationResponse {
  return {
    id: row.id,
    slug: row.slug,
    name: row.name,
    billing_email: null,
    billing_partner: null,
    integration_source: null,
    is_owner: true,
    opt_in_tags: [],
    organization_missing_address: false,
    organization_missing_tax_id: false,
    organization_requires_mfa: false,
    plan: ENTERPRISE_PLAN,
    restriction_data: null,
    restriction_status: null,
    stripe_customer_id: null,
    subscription_id: null,
    usage_billing_enabled: false,
  }
}

export function toOrganizationSlugResponse(row: PlatformOrganizationRow): OrganizationSlugResponse {
  return {
    id: row.id,
    slug: row.slug,
    name: row.name,
    billing_email: null,
    billing_partner: null,
    has_oriole_project: false,
    integration_source: null,
    opt_in_tags: [],
    organization_requires_mfa: false,
    plan: ENTERPRISE_PLAN,
    restriction_data: null,
    restriction_status: null,
    usage_billing_enabled: false,
  }
}

export async function listOrganizations(): Promise<PlatformOrganizationRow[]> {
  const { data, error } = await executePlatformQuery<PlatformOrganizationRow>({
    query: 'select id, slug, name from platform.organizations order by id',
  })
  if (error) throw error
  return data ?? []
}

export async function getOrganizationBySlug(slug: string): Promise<PlatformOrganizationRow | null> {
  const { data, error } = await executePlatformQuery<PlatformOrganizationRow>({
    query: 'select id, slug, name from platform.organizations where slug = $1',
    parameters: [slug],
  })
  if (error) throw error
  return data?.[0] ?? null
}
```

注意：若 `OrganizationResponse`/`OrganizationSlugResponse` 的必填字段与上述不完全一致，以 `pnpm --filter studio exec tsc --noEmit` 的报错为准补齐——**契约在 `packages/api-types/types/platform.d.ts:8663/8734`，不得用 `as any` 糊弄**。

- [ ] **Step 4: 改造两个 route**

`apps/studio/pages/api/platform/organizations/index.ts` handler 部分替换为：

```ts
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import { listOrganizations, toOrganizationResponse } from '@/lib/api/self-platform/organizations'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
  if (!IS_SELF_PLATFORM) return handleLegacyGetAll(req, res)

  const rows = await listOrganizations()
  return res.status(200).json(rows.map(toOrganizationResponse))
}

// Plain self-hosted keeps the historical stub payload untouched.
const handleLegacyGetAll = async (_req: NextApiRequest, res: NextApiResponse) => {
  const response = [
    {
      id: 1,
      name: process.env.DEFAULT_ORGANIZATION_NAME || 'Default Organization',
      slug: 'default-org-slug',
      billing_email: 'billing@supabase.co',
      plan: { id: 'enterprise', name: 'Enterprise' },
    },
  ]
  return res.status(200).json(response)
}
```

新建 `apps/studio/pages/api/platform/organizations/[slug]/index.ts`：

```ts
// [self-platform] Organization detail (OrganizationSlugResponse).
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import {
  getOrganizationBySlug,
  toOrganizationSlugResponse,
} from '@/lib/api/self-platform/organizations'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
  if (!IS_SELF_PLATFORM) {
    return res.status(404).json({ message: 'Not available on this deployment' })
  }

  const slug = String(req.query.slug)
  const row = await getOrganizationBySlug(slug)
  if (!row) return res.status(404).json({ message: 'Organization not found' })
  return res.status(200).json(toOrganizationSlugResponse(row))
}
```

- [ ] **Step 5: 跑测试 + 类型检查**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/organizations.test.ts`
Expected: PASS（4 tests）
Run: `cd /Volumes/data/projects/supabase && pnpm --filter studio exec tsc --noEmit 2>&1 | rg "self-platform|organizations" | head`
Expected: 无本任务相关报错（全量 tsc 慢/有既有噪音，只看相关行）

- [ ] **Step 6: Commit**

```bash
git add apps/studio/lib/api/self-platform/organizations.ts apps/studio/lib/api/self-platform/organizations.test.ts apps/studio/pages/api/platform/organizations/
git commit -m "feat(studio): organization endpoints backed by platform metadata db"
```

---

### Task 10: 项目列表端点（V2 分页形状 + 组织项目列表）

**Files:**

- Modify: `apps/studio/pages/api/platform/projects/index.ts`
- Create: `apps/studio/pages/api/platform/organizations/[slug]/projects.ts`
- Test: `apps/studio/pages/api/platform/projects/index.test.ts`

**Interfaces:**

- Consumes: `DEFAULT_PROJECT`（`@/lib/constants/api:13-24`）、`IS_SELF_PLATFORM`
- Produces（M1 仍单项目，M2 换注册表）:
  - `GET /platform/projects` + Header `Version: 2` → `{ pagination: {count:1, limit, offset}, projects: [projectListItem] }`；无 Version 头 → 既有 V1 数组（纯自托管不破坏）
  - `GET /platform/organizations/{slug}/projects` → 同分页形状，项目带 `databases: [{identifier: ref, ...PRIMARY}]`、`is_branch: false`
  - `projectListItem = { ...DEFAULT_PROJECT, organization_slug: 'default', preview_branch_refs: [] }`

- [ ] **Step 1: 写失败测试**

`apps/studio/pages/api/platform/projects/index.test.ts`：

```ts
import { createMocks } from 'node-mocks-http'
import { describe, expect, it, vi } from 'vitest'

import { handler } from './index'

vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', 'true')

describe('GET /platform/projects (self-platform)', () => {
  it('returns V2 paginated shape when Version: 2 header present', async () => {
    const { req, res } = createMocks({ method: 'GET', headers: { version: '2' } })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(200)
    const body = res._getJSONData()
    expect(body.pagination).toEqual({ count: 1, limit: expect.any(Number), offset: 0 })
    expect(body.projects[0]).toMatchObject({
      ref: 'default',
      organization_slug: 'default',
      preview_branch_refs: [],
      status: 'ACTIVE_HEALTHY',
    })
  })

  it('keeps the legacy V1 array without the header', async () => {
    const { req, res } = createMocks({ method: 'GET' })
    await handler(req as any, res as any)
    expect(Array.isArray(res._getJSONData())).toBe(true)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/projects/index.test.ts`
Expected: FAIL（handler 未导出 / V2 分支不存在）

- [ ] **Step 3: 实现**

`apps/studio/pages/api/platform/projects/index.ts` 替换为：

```ts
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import { DEFAULT_PROJECT } from '@/lib/constants/api'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

// [self-platform] exported for handler-level tests.
// M1: single default project wrapped in the V2 paginated contract; the
// project registry replaces this in M2.
export async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }

  const wantsV2 = IS_SELF_PLATFORM && req.headers['version'] === '2'
  if (!wantsV2) {
    return res.status(200).json([DEFAULT_PROJECT])
  }

  const limit = Number(req.query.limit ?? 100)
  const projects = [
    {
      ...DEFAULT_PROJECT,
      organization_slug: 'default',
      preview_branch_refs: [] as string[],
    },
  ]
  return res.status(200).json({
    pagination: { count: projects.length, limit, offset: Number(req.query.offset ?? 0) },
    projects,
  })
}
```

新建 `apps/studio/pages/api/platform/organizations/[slug]/projects.ts`：

```ts
// [self-platform] Org-scoped project list (used by org home + project
// selector). M1: the single default project; registry-backed in M2.
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'
import { DEFAULT_PROJECT } from '@/lib/constants/api'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
  if (!IS_SELF_PLATFORM) {
    return res.status(404).json({ message: 'Not available on this deployment' })
  }

  const projects = [
    {
      ...DEFAULT_PROJECT,
      organization_slug: 'default',
      is_branch: false,
      preview_branch_refs: [] as string[],
      databases: [
        {
          identifier: DEFAULT_PROJECT.ref,
          region: DEFAULT_PROJECT.region,
          status: DEFAULT_PROJECT.status,
          type: 'PRIMARY',
        },
      ],
    },
  ]
  return res.status(200).json({
    pagination: {
      count: projects.length,
      limit: Number(req.query.limit ?? 100),
      offset: Number(req.query.offset ?? 0),
    },
    projects,
  })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/studio && pnpm exec vitest run pages/api/platform/projects/index.test.ts`
Expected: PASS（2 tests）

- [ ] **Step 5: Commit**

```bash
git add "apps/studio/pages/api/platform/projects/index.ts" "apps/studio/pages/api/platform/projects/index.test.ts" "apps/studio/pages/api/platform/organizations/[slug]/projects.ts"
git commit -m "feat(studio): V2 project list shapes for self-platform mode"
```

---

### Task 11: Spike 清单驱动的 boot 兜底端点

**Files:**

- Create: `apps/studio/pages/api/platform/notifications.ts`（已知必需的工作样例，见下）
- Create: Task 1 findings 文档 `## 前端 boot 请求缺口` 一节中每个仍未被 Task 6–10 覆盖的路径对应的 route 文件
- Test: 无（纯契约空实现；形状由 `paths[...]` 类型保证）

**Interfaces:**

- Consumes: spike findings（Task 1）、`paths` 类型（`api-types`）
- Produces: boot 全流程无非预期 4xx/5xx。

- [ ] **Step 1: 对照清单**

打开 `docs/self-hosted-parity/2026-07-02-M1-spike-findings.md` 的缺口清单，划掉已被 Task 6–10 实现的路径，得到剩余清单。

- [ ] **Step 2: 按工作样例逐个补齐**

工作样例——`apps/studio/pages/api/platform/notifications.ts`（若 spike 显示该路径被调用；其余路径同款模式）：

```ts
// [self-platform] Contract-minimal stub: dashboard notifications are not
// implemented on self-platform (M1). Typed against api-types so upstream
// contract changes surface at compile time.
import type { paths } from 'api-types'
import { NextApiRequest, NextApiResponse } from 'next'

import apiWrapper from '@/lib/api/apiWrapper'

type NotificationsResponse =
  paths['/platform/notifications']['get']['responses']['200']['content']['application/json']

export default (req: NextApiRequest, res: NextApiResponse) =>
  apiWrapper(req, res, handler, { withAuth: true })

async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'GET') {
    res.setHeader('Allow', ['GET'])
    return res
      .status(405)
      .json({ data: null, error: { message: `Method ${req.method} Not Allowed` } })
  }
  const response: NotificationsResponse = []
  return res.status(200).json(response)
}
```

规则：

- 每个文件先用 `rg "'<path>'" packages/api-types/types/platform.d.ts` 找到确切 path key，`type X = paths['<path>']['get']['responses']['200']['content']['application/json']` 取形状；
- 返回该形状的**最小合法值**（空数组 / 全 null 可空对象）；若类型报错说明形状不对，修值不修类型；
- 找不到契约（非 platform.d.ts 内的路径）或前端对空值容忍（仅 console warning 不阻断）→ 记入 findings 文档「有意不实现」小节，不建文件。

- [ ] **Step 3: 复验**

以平台模式档案（Task 12 Step 1 的 env）起 dev server，重走 boot → 登录 → 项目页，确认 Network 面板无非预期 4xx/5xx；仍有的路径回到 Step 2 循环。

- [ ] **Step 4: Commit**

```bash
git add apps/studio/pages/api/platform/ docs/self-hosted-parity/2026-07-02-M1-spike-findings.md
git commit -m "feat(studio): contract-minimal boot stubs for self-platform mode"
```

---

### Task 12: 环境接线固化 + 端到端手动验收

**Files:**

- Create: `docker/volumes/platform/README.md`（platform 小栈 + Studio 平台模式接线文档）
- Modify: `apps/studio/.env.local`（切换为平台模式档案，此后常驻）

**Interfaces:**

- Consumes: 前面所有任务
- Produces: M1 验收通过记录（追加到 spike findings 文档末尾 `## M1 验收` 一节）

- [ ] **Step 1: 固化 Studio 平台模式 env 档案**

`apps/studio/.env.local` 最终档案（在既有 `PG_META_URL`/`PG_META_CRYPTO_KEY` 等值基础上追加）：

```bash
NEXT_PUBLIC_IS_PLATFORM=true
NEXT_PUBLIC_SELF_PLATFORM=true
NEXT_PUBLIC_API_URL=http://localhost:8082/api
NEXT_PUBLIC_GOTRUE_URL=http://localhost:8110
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=10000000-ffff-ffff-ffff-000000000001
PLATFORM_GOTRUE_URL=http://localhost:8110
PLATFORM_POSTGRES_HOST=platform-db
PLATFORM_POSTGRES_PORT=5432
PLATFORM_POSTGRES_DB=platform
PLATFORM_POSTGRES_USER=postgres
PLATFORM_POSTGRES_PASSWORD=<docker/.env 中的 PLATFORM_POSTGRES_PASSWORD>
```

- [ ] **Step 2: 写接线文档**

`docker/volumes/platform/README.md`：platform 小栈用途、启动命令、`.env` 必填变量（`PLATFORM_POSTGRES_PASSWORD`/`PLATFORM_JWT_SECRET`）、Studio 平台模式 env 档案（Step 1 全文）、首个管理员注册流程、以及「M1 边界：单项目、全权限、注册开放（M3 收紧）」。

- [ ] **Step 3: 跑全部 M1 单测**

```bash
cd /Volumes/data/projects/supabase/apps/studio
pnpm exec vitest run lib/hosted-api-allowlist.test.ts lib/api/self-hosted/util.test.ts lib/api/self-platform/ pages/api/platform/
```

Expected: 全部 PASS

- [ ] **Step 4: 端到端手动验收（spec §4.9 清单）**

前置：主栈 + platform 小栈运行中，`pnpm dev:studio`。逐项记录到 findings 文档 `## M1 验收`：

1. 未登录访问 `http://localhost:8082/project/default` → 重定向 `/sign-in?returnTo=...`；
2. `/sign-up` 注册新用户 → 成功（autoconfirm，无邮件）；
3. 登录 → 落到 `/organizations` 或 default org 页，组织列表显示 `Default Organization`；
4. 首登自动建档：`docker exec supabase-platform-db psql -U postgres -d platform -c "select username, primary_email from platform.profiles;"` 有该用户，且 `organization_members` 有对应行；
5. default 项目核心页全可用：Table Editor（读写一张表）、SQL Editor（跑 `select 1`）、Database、Auth、Storage、Logs；
6. 登出 → 回登录页；重新登录正常；
7. MFA：Account 设置里注册 TOTP → 登出重登 → 出现 MFA 挑战页并可通过；
8. API 鉴权底线：`curl -s -o /dev/null -w "%{http_code}" http://localhost:8082/api/platform/profile` → `401`；带登录 token（浏览器 localStorage `supabase.dashboard.auth.token` 里的 access_token）→ `200`；
9. 纯自托管回归：临时把 `.env.local` 换回 `.env.local.selfhosted.bak` 内容起 dev server → johndoe stub / `/project/default` 直达仍正常（验证零破坏），验完切回平台档案。

- [ ] **Step 5: 最终提交**

```bash
git add docker/volumes/platform/README.md docs/self-hosted-parity/2026-07-02-M1-spike-findings.md
git commit -m "docs: platform mini-stack wiring guide + M1 acceptance record"
```

---

## Self-Review 记录

- **Spec 覆盖**：§4.1→Task 4；§4.2→Task 4；§4.3→Task 12（env/hCaptcha）+ GoTrue 引擎自带（MFA）；§4.4→Task 2；§4.5→Task 6–11；§4.6→Task 6–10 全部 `withAuth: true` + 既有 `apiAuthenticate`（经共享 gotrue client 指向 platform-auth，无需改造——spike 验证）；§4.7/4.8→Task 6/7 错误形状 + Task 12 验收 8；§4.9→Task 12；§4.10→Task 1。
- **无占位符**：Task 11 的"逐个补齐"给出了完整工作样例 + 判定规则；其余任务代码齐全。
- **类型一致性**：`IS_SELF_PLATFORM`（Task 2 定义，3/6/7/8/9/10 消费）、`executePlatformQuery`（Task 5 定义，6/9 消费）、`PlatformProfileRow`/`PlatformOrganizationRow` 命名与字段在任务间一致；错误形状统一顶层 `message`。
