# F9+F16 M2.2（凭据闭合 + 卫生清扫）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 M2.1 终审 I1 遗留的 4 条全局凭据路由（config/index、config/postgrest、api/rest、api/graphql）按 ref 解析，并清扫 M2.1 终审的 5 项非阻塞卫生 follow-up，让 M3 从干净地基开始。

**Architecture:** 沿 M2.1 已工业化的 conn.row 门禁内联解析（T6 link 路由先例）：self-platform 命中注册表行 → 用行的 `jwtSecret`/`serviceKey`/`anonKey`/`supabaseUrl`；未注册 default 与纯自托管走今日 env 分支字节不变；未知 ref → ProjectNotFound → apiWrapper 兜底 404。四条路由今天零测试，各补 handler 测试 + sibling 零破坏测试。

**Tech Stack:** Next.js pages router API routes、resolveProjectConnection、vitest + node-mocks-http。

**Spec（已批准）:** `docs/self-hosted-parity/2026-07-03-F9-F16-M2.2-credential-closure-design.md`。分支 `feat/f9-f16-m2.2-credential-closure`（base custom/main @b1c904c9f4，spec 已提交其上 @9b01b6ca54）。

## Global Constraints

- 分支 `feat/f9-f16-m2.2-credential-closure`；Node >= 22，pnpm 10；提交 `feat(studio): ...` / `fix(studio): ...` / `docs: ...`。
- **resolved 行是唯一凭据源（硬约束）**：命中行后 `jwt_secret`/`apikey`/URL 只来自行，绝不回落全局 env。全局 env 只服务未注册 default（`conn.row === null`）与纯自托管。
- **纯自托管零破坏（硬约束）**：`IS_SELF_PLATFORM` 为 false 时逐字保留；每条被改路由配 sibling `*.self-hosted.test.ts`（动态 import 模式）。特别地：`endpoints/[name].ts` 的 assert→400 改进**只允许在 self-platform 分支**，plain 分支保留原 `assert` 逐字不动。
- 契约以 `packages/api-types` 为准（只读）；禁 `as any`（枚举窄化唯一例外）；错误体顶层 `{ message }`（route 级）；`ProjectNotFound` → 404 `{ message: 'Project not found' }`。
- 零新增 npm 依赖；`// [self-platform]` 标记；diff 最小化。
- 测试 env 注入：`vi.hoisted(() => { process.env.NEXT_PUBLIC_SELF_PLATFORM='true'; process.env.NEXT_PUBLIC_IS_PLATFORM='true' })`；同文件真实 import + importOriginal-spread mock 同一模块时，mock fn 用 `vi.hoisted()` 声明（Vitest 4.1.4 TDZ，M2.1 T6 先例）。
- 单测：`cd apps/studio && pnpm exec vitest run <file>`。2 个先前 TS6133 债务已在 M2.1 T12 清除——本里程碑 tsc 必须 0 错误。

## File Structure

| 文件                                                                                      | 职责                                             |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `pages/api/platform/projects/[ref]/config/index.ts`（改）                                 | GET jwt_secret 从 conn                           |
| `pages/api/platform/projects/[ref]/config/postgrest.ts`（改）                             | GET jwt_secret 从 conn                           |
| `pages/api/platform/projects/[ref]/api/rest.ts`（改）                                     | GET 代理注册表 REST URL + serviceKey             |
| `pages/api/platform/projects/[ref]/api/graphql.ts`（改）                                  | POST 代理注册表 graphql URL + serviceKey/anonKey |
| `apps/studio/lib/api/self-platform/projects.ts`（改）                                     | 缺列降级 once-guard + console.warn               |
| `apps/studio/lib/api/self-platform/pagination.ts`（改）                                   | limit 上限钳制 1000                              |
| `pages/api/platform/projects/[ref]/api-keys/temporary.ts`（改）                           | 非字符串 role → 400                              |
| `pages/api/platform/projects/[ref]/analytics/log-drains.ts`、`log-drains/[uuid].ts`（改） | AssertionError → 描述性 500                      |
| `pages/api/platform/projects/[ref]/analytics/endpoints/[name].ts`（改）                   | self-platform 分支 array/missing ref/name → 400  |
| `docker/volumes/platform/README.md`（改）                                                 | 共享栈 JWT-secret + mcp.ts 不对称 + 边界更新     |
| 各路由 sibling `.test.ts` / `.self-hosted.test.ts`（新）                                  | handler + 零破坏                                 |

**依赖**：T1–T4 相互独立，可乱序；T5（文档+验收）依赖前四者落地。

---

### Task 1: config 两路由 per-ref

**Files:**

- Modify: `apps/studio/pages/api/platform/projects/[ref]/config/index.ts`、`config/postgrest.ts`
- Create: 同目录 `index.test.ts`、`index.self-hosted.test.ts`、`postgrest.test.ts`、`postgrest.self-hosted.test.ts`

**Interfaces:**

- Consumes: `resolveProjectConnection(ref)`（conn.jwtSecret / conn.row）、`ProjectNotFound`、`IS_SELF_PLATFORM`。
- Produces: 两路由 GET 在 self-platform 命中行时 `jwt_secret = conn.jwtSecret`，其余字段保持现值；未知 ref 404；纯自托管字节不变。config/index PATCH no-op 不动。

- [ ] **Step 1: 写失败测试（config/index）**

`config/index.test.ts`（开态）：

```ts
import { createMocks } from 'node-mocks-http'
import { beforeEach, describe, expect, it, vi } from 'vitest'

import { handler } from './index'
import { ProjectNotFound } from '@/lib/api/self-platform/resolve-connection'

vi.hoisted(() => {
  process.env.NEXT_PUBLIC_SELF_PLATFORM = 'true'
  process.env.NEXT_PUBLIC_IS_PLATFORM = 'true'
})

const resolveProjectConnection = vi.fn()
vi.mock('@/lib/api/self-platform/resolve-connection', async (importOriginal) => ({
  ...(await importOriginal<object>()),
  resolveProjectConnection,
}))

beforeEach(() => resolveProjectConnection.mockReset())

describe('GET /platform/projects/{ref}/config (self-platform)', () => {
  it('returns the resolved project jwt_secret, keeping non-secret fields', async () => {
    resolveProjectConnection.mockResolvedValueOnce({ row: { id: 2 }, jwtSecret: 'secret-b' })
    const { req, res } = createMocks({ method: 'GET', query: { ref: 'proj-b' } })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(200)
    const body = res._getJSONData()
    expect(body.jwt_secret).toBe('secret-b')
    expect(body.db_anon_role).toBe('anon')
    expect(body.db_schema).toBe('public, storage')
    expect(body.max_rows).toBe(100)
  })

  it('404s unknown ref', async () => {
    resolveProjectConnection.mockRejectedValueOnce(new ProjectNotFound('ghost'))
    const { req, res } = createMocks({ method: 'GET', query: { ref: 'ghost' } })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(404)
    expect(res._getJSONData()).toEqual({ message: 'Project not found' })
  })
})
```

- [ ] **Step 2: 跑测确认失败**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/config/index.test.ts"`
Expected: FAIL —— handler 未导出 / jwt_secret 仍是全局 env。

- [ ] **Step 3: 实现 config/index.ts**

import 区加：

```ts
import {
  ProjectNotFound,
  resolveProjectConnection,
} from '@/lib/api/self-platform/resolve-connection'
import { IS_SELF_PLATFORM } from '@/lib/constants/self-platform'
```

`handler` 加 `export`（`export async function handler`）。`handleGetAll` 改为：

```ts
const handleGetAll = async (req: NextApiRequest, res: NextApiResponse) => {
  // [self-platform] jwt_secret must come from the resolved project; other
  // fields are stack-level PostgREST config the registry doesn't model, so
  // they keep their historical values (mirrors getProjectSettings).
  let jwtSecret =
    process.env.AUTH_JWT_SECRET ?? 'super-secret-jwt-token-with-at-least-32-characters-long'
  if (IS_SELF_PLATFORM) {
    try {
      const conn = await resolveProjectConnection(String(req.query.ref))
      if (conn.row) jwtSecret = conn.jwtSecret
    } catch (err) {
      if (err instanceof ProjectNotFound) {
        return res.status(404).json({ message: 'Project not found' })
      }
      throw err
    }
  }
  return res.status(200).json({
    db_anon_role: 'anon',
    db_extra_search_path: 'public',
    db_schema: 'public, storage',
    jwt_secret: jwtSecret,
    max_rows: 100,
    role_claim_key: '.role',
  })
}
```

（`handleGetAll` 现在用 `req`，去掉 `_req` 下划线。PATCH `handlePatch` 不动。default export 行不变。）

- [ ] **Step 4: 写失败测试（config/postgrest，同构）**

`postgrest.test.ts`：结构同 Step 1，断言 `jwt_secret` 从 conn、`db_schema` 保持 `process.env.PGRST_DB_SCHEMAS ?? 'public,storage,graphql_public'` 的推导值（测试里不 stub PGRST\_\* 时即默认字符串），未知 ref 404。

- [ ] **Step 5: 实现 config/postgrest.ts**

同 Step 3 的解析骨架；`responseObj.jwt_secret` 改为解析出的 `jwtSecret`（命中行→conn.jwtSecret，否则现有 env 默认）。其余字段（含 PGRST\_\* env 读取）逐字保留。`export async function handler`。

- [ ] **Step 6: 写零破坏 sibling（两文件）**

`index.self-hosted.test.ts` / `postgrest.self-hosted.test.ts`（关态，动态 import 模式）：`vi.resetModules()` + `vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM','')` + stub `AUTH_JWT_SECRET='env-secret'`，断言 (a) `resolveProjectConnection`（mock）未被调用；(b) 响应与今日字面一致（`jwt_secret === 'env-secret'`，其余字段等于现值）。示例（index）：

```ts
import { createMocks } from 'node-mocks-http'
import { afterEach, describe, expect, it, vi } from 'vitest'

const resolveProjectConnection = vi.fn()
vi.mock('@/lib/api/self-platform/resolve-connection', async (importOriginal) => ({
  ...(await importOriginal<object>()),
  resolveProjectConnection,
}))

afterEach(() => vi.unstubAllEnvs())

describe('config (plain self-hosted, zero-break)', () => {
  it('returns global env jwt_secret; resolver untouched', async () => {
    vi.resetModules()
    vi.stubEnv('NEXT_PUBLIC_SELF_PLATFORM', '')
    vi.stubEnv('AUTH_JWT_SECRET', 'env-secret')
    const { handler } = await import('./index')
    const { req, res } = createMocks({ method: 'GET', query: { ref: 'default' } })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(200)
    expect(res._getJSONData().jwt_secret).toBe('env-secret')
    expect(resolveProjectConnection).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 7: 跑测确认通过**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/config/"`
Expected: 全 PASS。

- [ ] **Step 8: Commit**

```bash
git add "apps/studio/pages/api/platform/projects/[ref]/config/"
git commit -m "fix(studio): config routes return per-ref jwt_secret"
```

---

### Task 2: api/rest + api/graphql per-ref

**Files:**

- Modify: `apps/studio/pages/api/platform/projects/[ref]/api/rest.ts`、`api/graphql.ts`
- Create: 同目录 `rest.test.ts`、`rest.self-hosted.test.ts`、`graphql.test.ts`、`graphql.self-hosted.test.ts`

**Interfaces:**

- Consumes: `resolveProjectConnection`（conn.supabaseUrl / serviceKey / anonKey / row）、`ProjectNotFound`、`IS_SELF_PLATFORM`。
- Produces: rest GET 对命中行 fetch `${conn.supabaseUrl}/rest/v1/` + `apikey: conn.serviceKey`；graphql POST fetch `${conn.supabaseUrl}/graphql/v1` + `apikey: conn.serviceKey`、Authorization 回退 `Bearer conn.anonKey`；未知 ref 404；纯自托管字节不变。rest HEAD 不动。

- [ ] **Step 1: 写失败测试（rest）**

`rest.test.ts`（开态，global fetch 用 `vi.stubGlobal`）：

```ts
import { createMocks } from 'node-mocks-http'
import { beforeEach, describe, expect, it, vi } from 'vitest'

import { handler } from './rest'
import { ProjectNotFound } from '@/lib/api/self-platform/resolve-connection'

vi.hoisted(() => {
  process.env.NEXT_PUBLIC_SELF_PLATFORM = 'true'
  process.env.NEXT_PUBLIC_IS_PLATFORM = 'true'
})

const resolveProjectConnection = vi.fn()
vi.mock('@/lib/api/self-platform/resolve-connection', async (importOriginal) => ({
  ...(await importOriginal<object>()),
  resolveProjectConnection,
}))

beforeEach(() => resolveProjectConnection.mockReset())
afterEach?.(() => vi.unstubAllGlobals())

describe('GET /platform/projects/{ref}/api/rest (self-platform)', () => {
  it('proxies the resolved project REST url with its service key', async () => {
    resolveProjectConnection.mockResolvedValueOnce({
      row: { id: 2 },
      supabaseUrl: 'http://kong-b:8100',
      serviceKey: 'service-b',
    })
    const fetchMock = vi.fn().mockResolvedValue({ ok: true, json: async () => ({ paths: {} }) })
    vi.stubGlobal('fetch', fetchMock)
    const { req, res } = createMocks({ method: 'GET', query: { ref: 'proj-b' } })
    await handler(req as any, res as any)
    expect(fetchMock.mock.calls[0][0]).toBe('http://kong-b:8100/rest/v1/')
    expect(fetchMock.mock.calls[0][1].headers.apikey).toBe('service-b')
    expect(res._getStatusCode()).toBe(200)
    vi.unstubAllGlobals()
  })

  it('404s unknown ref before fetching', async () => {
    resolveProjectConnection.mockRejectedValueOnce(new ProjectNotFound('ghost'))
    const fetchMock = vi.fn()
    vi.stubGlobal('fetch', fetchMock)
    const { req, res } = createMocks({ method: 'GET', query: { ref: 'ghost' } })
    await handler(req as any, res as any)
    expect(res._getStatusCode()).toBe(404)
    expect(fetchMock).not.toHaveBeenCalled()
    vi.unstubAllGlobals()
  })
})
```

（`afterEach?.` 写法避免 import 顺序问题；实现时用标准 `import { afterEach } from 'vitest'` 并正常调用。）

- [ ] **Step 2: 跑测确认失败**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/api/rest.test.ts"`
Expected: FAIL。

- [ ] **Step 3: 实现 rest.ts**

import 区加 resolver 两导出 + `IS_SELF_PLATFORM`；`export async function handler`。`handleGet` 改为：

```ts
const handleGet = async (req: NextApiRequest, res: NextApiResponse) => {
  // [self-platform] Proxy the resolved project's REST endpoint; plain
  // self-hosted keeps the global env target byte-identically.
  let restUrl = `${process.env.SUPABASE_URL}/rest/v1/`
  let apikey = process.env.SUPABASE_SERVICE_KEY!
  if (IS_SELF_PLATFORM) {
    try {
      const conn = await resolveProjectConnection(String(req.query.ref))
      if (conn.row) {
        restUrl = `${conn.supabaseUrl}/rest/v1/`
        apikey = conn.serviceKey
      }
    } catch (err) {
      if (err instanceof ProjectNotFound) {
        return res.status(404).json({ message: 'Project not found' })
      }
      throw err
    }
  }
  const response = await fetch(restUrl, { method: 'GET', headers: { apikey } })
  if (response.ok) {
    const data = await response.json()
    return res.status(200).json(data)
  }
  return res.status(500).json({ error: { message: 'Internal Server Error' } })
}
```

（`handleHead` 不动；`handleGet` 现在用 `req`。）

- [ ] **Step 4: 写失败测试（graphql）**

`graphql.test.ts`（开态）：断言 resolved POST fetch `${conn.supabaseUrl}/graphql/v1`、`apikey === conn.serviceKey`、无 `x-graphql-authorization` 时 Authorization === `Bearer ${conn.anonKey}`；传了该 header 时透传；未知 ref 404 且不 fetch。

- [ ] **Step 5: 实现 graphql.ts**

同骨架。`handleGet`（POST 分支）解析后：

```ts
  let gqlUrl = `${process.env.SUPABASE_URL}/graphql/v1`
  let apikey = process.env.SUPABASE_SERVICE_KEY!
  let anonKey = process.env.SUPABASE_ANON_KEY
  if (IS_SELF_PLATFORM) {
    try {
      const conn = await resolveProjectConnection(String(req.query.ref))
      if (conn.row) {
        gqlUrl = `${conn.supabaseUrl}/graphql/v1`
        apikey = conn.serviceKey
        anonKey = conn.anonKey ?? undefined
      }
    } catch (err) {
      if (err instanceof ProjectNotFound) {
        return res.status(404).json({ message: 'Project not found' })
      }
      throw err
    }
  }
  const authorizationHeader = req.headers['x-graphql-authorization']
  const response = await fetch(gqlUrl, {
    method: 'POST',
    headers: {
      apikey,
      Authorization:
        (Array.isArray(authorizationHeader) ? authorizationHeader[0] : authorizationHeader) ??
        `Bearer ${anonKey}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(req.body),
  })
  ...（其余逐字不变）
```

- [ ] **Step 6: 写零破坏 siblings（两文件）**

`rest.self-hosted.test.ts` / `graphql.self-hosted.test.ts`：关态动态 import，stub `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/`SUPABASE_ANON_KEY` + mock global fetch，断言 fetch 打到全局 URL + 全局 key、resolver 未被调用。

- [ ] **Step 7: 跑测确认通过**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/api/"`
Expected: 全 PASS。

- [ ] **Step 8: Commit**

```bash
git add "apps/studio/pages/api/platform/projects/[ref]/api/"
git commit -m "fix(studio): rest and graphql proxy routes resolve per-ref"
```

---

### Task 3: analytics 卫生（drains AssertionError 映射 + endpoints 参数 400）

**Files:**

- Modify: `pages/api/platform/projects/[ref]/analytics/log-drains.ts`、`log-drains/[uuid].ts`、`endpoints/[name].ts`
- Modify: `pages/api/platform/projects/[ref]/analytics/per-ref.test.ts`（追加用例）
- Create: `endpoints/[name].self-hosted.test.ts`（若不存在）

**Interfaces:**

- Consumes: 现有 `getAnalyticsTarget`（未注册 default 回落时 `assert(process.env.LOGFLARE_URL)` 抛 `AssertionError`）、`IS_SELF_PLATFORM`。
- Produces:
  - 两条 drains 路由 self-platform catch 增加 `AssertionError` 分支 → 500 `{ error: { message: err.message } }`（与 plain 分支描述性错误形状一致，取代裸 `{ error: AssertionError }`）。
  - `endpoints/[name].ts`：self-platform 分支 array/missing `ref`/`name` → 400 `{ message }`；plain 分支保留原 `assert` 逐字。

- [ ] **Step 1: 写失败测试（drains AssertionError）**

`analytics/per-ref.test.ts` 追加（log-drains，getAnalyticsTarget mock 抛 AssertionError）：

```ts
import assert from 'node:assert'

it('maps analytics-target AssertionError to a descriptive 500', async () => {
  let assertionError: Error
  try {
    assert(false, 'LOGFLARE_URL is required')
  } catch (e) {
    assertionError = e as Error
  }
  getAnalyticsTarget.mockRejectedValueOnce(assertionError!)
  const { handler } = await import('./log-drains')
  const { req, res } = createMocks({ method: 'GET', query: { ref: 'default' } })
  await handler(req as any, res as any)
  expect(res._getStatusCode()).toBe(500)
  expect(res._getJSONData()).toEqual({ error: { message: 'LOGFLARE_URL is required' } })
})
```

- [ ] **Step 2: 跑测确认失败**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/analytics/per-ref.test.ts"`
Expected: 新用例 FAIL（AssertionError 现在走 `throw err` → apiWrapper 500 `{ error }` 裸对象，非 `{ error: { message } }`）。

- [ ] **Step 3: 实现两条 drains 路由**

`log-drains.ts` 与 `log-drains/[uuid].ts` 的 self-platform catch 块，在 `AnalyticsNotConfigured` 分支后、`throw err` 前加：

```ts
// [self-platform] Unregistered-default fallback with missing LOGFLARE_*
// env throws AssertionError from getAnalyticsTarget — surface the
// descriptive message like the plain-self-hosted branch does, not a
// bare error object.
if (err instanceof Error && err.name === 'AssertionError') {
  return res.status(500).json({ error: { message: err.message } })
}
```

- [ ] **Step 4: 写失败测试（endpoints/[name] 参数 400）**

`analytics/per-ref.test.ts` 追加（self-platform）：

```ts
it.each([
  [{ ref: ['a', 'b'], name: 'logs.all' }, 'Invalid ref parameter'],
  [{ ref: 'proj-b' }, 'Invalid name parameter'],
])('400s malformed params on endpoints/[name]: %j', async (query, message) => {
  const { handler } = await import('./endpoints/[name]')
  const { req, res } = createMocks({ method: 'GET', query })
  await handler(req as any, res as any)
  expect(res._getStatusCode()).toBe(400)
  expect(res._getJSONData()).toEqual({ message })
})
```

- [ ] **Step 5: 实现 endpoints/[name].ts**

import 区加 `IS_SELF_PLATFORM`。GET/POST case 的两条 `assert` 替换为 self-platform 门禁校验（plain 保留 assert）：

```ts
    case 'GET':
    case 'POST': {
      const { name, ref, ...queryToForward } = req.query
      const params = req.method === 'GET' ? queryToForward : req.body

      if (IS_SELF_PLATFORM) {
        // [self-platform] 400 on malformed params, consistent with the
        // milestone's other self-platform validation. Plain self-hosted keeps
        // the original asserts below byte-identically.
        if (typeof ref !== 'string') {
          return res.status(400).json({ message: 'Invalid ref parameter' })
        }
        if (typeof name !== 'string') {
          return res.status(400).json({ message: 'Invalid name parameter' })
        }
      } else {
        assert(typeof ref === 'string', 'Invalid or missing ref parameter')
        assert(typeof name === 'string', 'Invalid or missing name parameter')
      }

      try {
        const { data, error } = await retrieveAnalyticsData({
          name: name as string,
          params,
          projectRef: ref as string,
        })
        ...（try 块其余逐字不变）
```

（`name as string`/`ref as string` 是控制流窄化后的赘余，但 plain 分支的 assert 不改变 TS 的 union 推断——用 `String(name)`/`String(ref)` 也可，二选一保持 tsc 干净；实现者以 tsc 通过为准，禁 `as any`。）

- [ ] **Step 6: 写 plain 零破坏 sibling**

`endpoints/[name].self-hosted.test.ts`：关态动态 import，断言数组 ref 触发原 assert 路径（AssertionError → apiWrapper 500 `{ error }`），证明 plain 分支未被 400 改动影响。

- [ ] **Step 7: 跑测确认通过**

Run: `cd apps/studio && pnpm exec vitest run "pages/api/platform/projects/[ref]/analytics/"`
Expected: 全 PASS。

- [ ] **Step 8: Commit**

```bash
git add "apps/studio/pages/api/platform/projects/[ref]/analytics/"
git commit -m "fix(studio): analytics drains descriptive 500 and endpoints param 400"
```

---

### Task 4: 杂项卫生（降级 once-guard + temporary role + 分页钳制）

**Files:**

- Modify: `apps/studio/lib/api/self-platform/projects.ts`（once-guard）
- Modify: `apps/studio/pages/api/platform/projects/[ref]/api-keys/temporary.ts`（role 严格）
- Modify: `apps/studio/lib/api/self-platform/pagination.ts`（上限钳制）
- Modify: `apps/studio/lib/api/self-platform/projects.analytics-columns.test.ts`（once-guard 用例）
- Modify: `apps/studio/pages/api/platform/projects/[ref]/api-keys/temporary.test.ts`（role 用例）
- Modify: `apps/studio/lib/api/self-platform/pagination.test.ts`（clamp 用例）

**Interfaces:**

- Produces:
  - `projects.ts`：模块级 `warnedMissingAnalyticsColumns` 一次性守卫，`console.warn` 替代每次 `console.log`；降级重试逻辑不变。
  - `temporary.ts`：claims 合法 JSON 且 `role` 存在但非字符串 → 400。
  - `parsePaginationParam`：返回值 > 1000 钳制为 1000（null/负/非整数判定不变）。

- [ ] **Step 1: 写失败测试（三处）**

`projects.analytics-columns.test.ts` 追加（在已 mock `./db` 的 describe 内）:

```ts
it('warns only once across repeated degraded reads', async () => {
  const warn = vi.spyOn(console, 'warn').mockImplementation(() => {})
  const legacyErr = { data: undefined, error: new Error('column "logflare_url" does not exist') }
  const legacyRow = {
    /* 任一 legacy 行，见文件既有 fullRow 去掉两列 */
  }
  mockQuery
    .mockResolvedValueOnce(legacyErr)
    .mockResolvedValueOnce({ data: [legacyRow], error: undefined })
    .mockResolvedValueOnce(legacyErr)
    .mockResolvedValueOnce({ data: [legacyRow], error: undefined })
  await getProjectByRef('proj-b')
  await getProjectByRef('proj-b')
  expect(warn).toHaveBeenCalledTimes(1)
  warn.mockRestore()
})
```

（注意：once-guard 是模块级状态，本用例需 `vi.resetModules()` + 重新 import，或放在文件首个降级用例——实现者确保与既有降级用例的调用次序不互相污染；必要时该用例用独立 `vi.isolateModules`。）

`temporary.test.ts` 追加：

```ts
it('400s a valid-JSON claims object whose role is not a string', async () => {
  resolveProjectConnection.mockResolvedValue({ row: { id: 2 }, jwtSecret: 's' })
  const { req, res } = createMocks({
    method: 'POST',
    query: { ref: 'proj-b', claims: JSON.stringify({ role: 123 }) },
  })
  await handler(req as any, res as any)
  expect(res._getStatusCode()).toBe(400)
})
```

`pagination.test.ts` 追加：

```ts
it('clamps limit above 1000 to 1000', () => {
  expect(parsePaginationParam('5000', 100)).toBe(1000)
  expect(parsePaginationParam('1000', 100)).toBe(1000)
  expect(parsePaginationParam('999', 100)).toBe(999)
})
```

- [ ] **Step 2: 跑测确认失败**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/projects.analytics-columns.test.ts lib/api/self-platform/pagination.test.ts "pages/api/platform/projects/[ref]/api-keys/temporary.test.ts"`
Expected: 三个新用例 FAIL。

- [ ] **Step 3: 实现 projects.ts once-guard**

文件顶部（`MISSING_ANALYTICS_COLUMN` 常量后）加：

```ts
let warnedMissingAnalyticsColumns = false
```

`queryProjectRows` 里的 `console.log(...)` 块替换为：

```ts
if (!warnedMissingAnalyticsColumns) {
  warnedMissingAnalyticsColumns = true
  console.warn(
    '[self-platform] platform.projects has no analytics columns (pre-M2.1 platform-db) — treating logflare_url/logflare_token_enc as NULL. Run docker/volumes/platform/migrations/03-analytics.sql to upgrade.'
  )
}
```

- [ ] **Step 4: 实现 temporary.ts role 严格**

现有 claims 解析块（`if (typeof parsed?.role === 'string') role = parsed.role`）替换为：

```ts
try {
  const parsed = JSON.parse(rawClaims)
  if (parsed?.role !== undefined) {
    if (typeof parsed.role !== 'string') {
      return res.status(400).json({ message: 'Invalid claims parameter' })
    }
    role = parsed.role
  }
} catch {
  return res.status(400).json({ message: 'Invalid claims parameter' })
}
```

- [ ] **Step 5: 实现 pagination.ts 钳制**

`parsePaginationParam` 末尾 return 改为：

```ts
return Math.min(parseInt(value, 10), 1000)
```

并更新文件顶部注释，说明上限 1000（钳制而非拒绝）。

- [ ] **Step 6: 跑测确认通过**

Run: `cd apps/studio && pnpm exec vitest run lib/api/self-platform/ "pages/api/platform/projects/[ref]/api-keys/"`
Expected: 全 PASS（含既有分页/降级/temporary 用例不回归）。

- [ ] **Step 7: Commit**

```bash
git add apps/studio/lib/api/self-platform/projects.ts apps/studio/lib/api/self-platform/pagination.ts \
  apps/studio/lib/api/self-platform/projects.analytics-columns.test.ts \
  apps/studio/lib/api/self-platform/pagination.test.ts \
  "apps/studio/pages/api/platform/projects/[ref]/api-keys/temporary.ts" \
  "apps/studio/pages/api/platform/projects/[ref]/api-keys/temporary.test.ts"
git commit -m "fix(studio): degradation warn-once, strict temporary role, pagination clamp"
```

---

### Task 5: README + 全量验证 + E2E

**Files:**

- Modify: `docker/volumes/platform/README.md`

**Interfaces:**

- Consumes: T1–T4 全部落地。
- Produces: 文档 + 运行验证；不改产品代码（发现问题 → 开修复任务）。

- [ ] **Step 1: README 更新**

`docker/volumes/platform/README.md`：

1. **边界章节**：从"仍返回全局凭据"清单移除 `config/*` 与 `api/rest|graphql`（已修复），归入 M2.2 已修列表。
2. **新增 "Shared-stack JWT secret" 说明**：同一栈注册的多个项目共用 JWT secret，故为某 ref 铸的临时 JWT 在其兄弟项目上也可验签——共享栈注册的固有属性，独立 secret 需独立栈。（M3 RBAC 预期在此设定。）
3. **新增 "MCP per-ref asymmetry" 说明**：self-platform 下 MCP 的 `getLogs` 已经 `retrieveAnalyticsData` 按 ref 解析，而 advisors/lints 查询仍打全局 DB；projectRef 穿线延后到 MCP 多项目化。

- [ ] **Step 2: 全量验证（记录输出）**

```bash
cd apps/studio && pnpm exec vitest run 2>&1 | tail -12
pnpm exec tsc --noEmit; echo "tsc exit: $?"
cd ../.. && pnpm lint --filter=studio 2>&1 | tail -6
```

Expected: vitest 全绿（4422 基线 + 本里程碑新增）；tsc exit 0；lint exit 0。三段输出贴进 report。

- [ ] **Step 3: E2E 验收（双项目环境，控制器执行）**

前置：主栈 :8100 + platform 小栈在跑；`lsof -ti:8082 | xargs kill -9` 后 `pnpm dev:studio`；平台 GoTrue :8110 登录取 token。逐项验证：

1. **config jwt_secret per-ref**：`GET /api/platform/projects/proj-b/config/postgrest` 的 `jwt_secret` == 注册表 proj-b 行解密值（共享栈下等于 default 的——预期）；ghost → 404。
2. **rest/graphql per-ref**：`GET /api/platform/projects/proj-b/api/rest` 打注册表 URL（200，返回 OpenAPI 形状）；ghost → 404 不 fetch。
3. **卫生**：temporary `claims={"role":123}` → 400；`/projects?limit=5000` 的 pagination.limit == 1000。
4. **纯自托管回归**：旗标切 false 重启，config/rest 返回全局 env 值、temporary 返回裸 service key，切回。

- [ ] **Step 4: Commit（docs-only）**

```bash
git add docker/volumes/platform/README.md
git commit -m "docs: M2.2 credential-closure boundary, shared-stack JWT, mcp asymmetry"
```

---

## Self-Review 记录（计划作者自查）

- **Spec 覆盖**：spec §4.1 四路由→T1/T2；§4.2-1 once-guard→T4；§4.2-2 role→T4；§4.2-3 drains→T3；§4.2-4 endpoints 400→T3；§4.2-5 分页钳制→T4；§5 文档→T5；§6 测试→各任务测试步 + T5。
- **类型一致性**：四路由统一 `conn.row` 门禁 + `ProjectNotFound`→404 骨架（与 M2.1 T6/T9 逐字同构）；`parsePaginationParam` 签名不变（仅返回值钳制）。
- **零破坏红线**：endpoints/[name] 的 400 严格门禁在 `IS_SELF_PLATFORM` 分支内，plain 保留 assert，T3 Step 6 sibling 专门守这条；四路由各有 self-hosted sibling。
- **占位符**：无 TBD；once-guard 测试的模块级状态污染风险已在 T4 Step 1 显式提示（isolateModules/resetModules）。
